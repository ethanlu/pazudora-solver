language: python
python: 2.7
install:
  - pip install tox
  - pip install coveralls
  - pip install codacy-coverage
script:
  - tox
after_success:
  - python-codacy-coverage -r coverage.xml
  - coveralls
sudo: false